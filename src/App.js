/* eslint-disable react/prop-types */
import React from 'react';
import ProductProvider from './ProductProvider';

const App = (props) => {
  return (
    <div>
      <ProductProvider />
    </div>
  );
};

export default App;
