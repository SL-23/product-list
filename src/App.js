/* eslint-disable react/prop-types */
import React from 'react';
import Layout from './hoc/Layout';

const App = (props) => {
  return (
   <div>
     <Layout/>
   </div>
  );
};

export default App;
